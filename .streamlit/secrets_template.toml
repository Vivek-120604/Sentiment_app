# Copy this template and add your real values in Streamlit Cloud Secrets
# Do NOT commit real tokens to the repository.

# Hugging Face API token (recommended). Example: "hf_xxx..."
HF_API_TOKEN = "<your_hf_api_token_here>"

# Optional: external model & word index URLs (if you host artifacts elsewhere)
# MODEL_URL = "https://example.com/simple_rnn_imdb.keras"
# WORD_INDEX_URL = "https://example.com/word_to_index.pkl"
